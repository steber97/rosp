Code experiments for the paper "ROSP: certifying positive semidefiniteness through the rank-one shift problem"

# Install

Clone the repository, `cd rosp` and install the requirements:

```pip install -r requirements.txt```

# Reproduce the plots

Run the experiment

```python src/experiment.py```

The results will appear in the folder `figures`.
